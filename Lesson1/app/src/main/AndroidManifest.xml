<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lesson1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MyActivity" android:label="The First Activity"
            android:theme="@style/AlertDialog.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".SecondActivity" android:label="The Second Activity"
            android:theme="@style/AlertDialog.AppCompat">
            <intent-filter>
                <action android:name="LaunchSecondActivity"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!--<activity android:name=".ThirdActivity" android:label="The Third Activity">-->
            <!--<intent-filter>-->
                <!--<action android:name="LaunchSecondActivity"/>-->
                <!--<category android:name="android.intent.category.DEFAULT"/>-->
            <!--</intent-filter>-->

        <!--</activity>-->
    </application>

</manifest>
